{
  "name": "dm-toolkit",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/dm-toolkit",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/dm-toolkit",
        "outputPath": "dist/apps/dm-toolkit"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/dm-toolkit"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "dm-toolkit:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "dm-toolkit:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "dm-toolkit:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "dm-toolkit:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/dm-toolkit/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/dm-toolkit/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
