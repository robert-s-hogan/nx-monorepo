{
  "name": "daoism",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/daoism",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/daoism",
        "outputPath": "dist/apps/daoism"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/daoism"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "daoism:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "daoism:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "daoism:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "daoism:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/apps/daoism"],
      "options": {
        "jestConfig": "apps/daoism/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/daoism/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
