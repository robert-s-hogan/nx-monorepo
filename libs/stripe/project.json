{
  "name": "stripe",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "with-nx/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/with-nx",
        "main": "with-nx/src/index.ts",
        "tsConfig": "with-nx/tsconfig.lib.json",
        "assets": ["with-nx/*.md"]
      }
    }
  }
}
